[pytest]
# Local pytest configuration for blocks directory
# This prevents namespace conflicts with the root-level tests directory
testpaths = .
python_files = *_test_.py
pythonpath = ..
# Explicitly disable test discovery in other directories
norecursedirs = ../tests ../pkgs/atlasvibe/tests
